Algoritmo "fatorial"

Var
     N, i, fatorial : inteiro

Inicio

      escreva("Digite o valor de N: ")
      leia(N)
      
      para i de 0 ate N faca
      
      fatorial <- (N * (N - 1)) / 2
      fimpara
      
      escreva("FATORIAL = ", fatorial)


Fimalgoritmo