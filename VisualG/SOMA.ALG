Algoritmo "soma"

Var

   x : inteiro
   y : inteiro
   soma : Inteiro


Inicio

   escreval("Digite o valor de X: ")
   leia(x)
   escreval("Digite o valor de y: ")
   leia (y)
   
   soma <- (x + y)
   escreval
   escreval("SOMA = ",soma)
   

Fimalgoritmo