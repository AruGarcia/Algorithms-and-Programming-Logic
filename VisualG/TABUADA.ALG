Algoritmo "tabuada"

Var

 i, x, y : inteiro

Inicio

      escreva("Deseja a tabuada para qual valor? ")
      leia(y)

      para i de 1 ate 10 faca
      x <- Y * i
      escreval(y," x", i, " = ", x)
      fimpara



Fimalgoritmo